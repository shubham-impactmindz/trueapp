<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active track (light green) -->
    <item android:color="@color/colorGreenLight" android:state_checked="true"/>
    <!-- Inactive track (light secondary) -->
    <item android:color="@color/colorSecondaryOpacity" android:state_checked="false"/>
</selector>
